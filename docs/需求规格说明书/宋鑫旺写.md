# 需求规格说明书 · 宋鑫旺负责部分

---

## 1.1 项目背景与目标

<!-- TODO：写 200-300 字，说明为什么要做这个系统，解决什么问题 -->

**项目背景：**

（校园流浪动物的现状、管理问题、学生需求）

**项目目标：**

（开发一个什么样的系统，核心价值是什么）

---

## 1.2 用户角色定义

<!-- TODO：定义系统有哪些用户角色，各自的特点 -->

| 角色 | 描述 | 典型用户 |
|:----|:-----|:--------|
| 管理员 | | |
| 学生/游客 | | |

---

## 2.2 数据流图 (DFD)

### 顶层图（上下文图）

<!-- TODO：用文字描述顶层图，包含外部实体、系统边界、主要数据流 -->
<!-- 格式建议：
外部实体：[管理员, 学生/游客]
系统：校园流浪动物观测关爱系统
数据流：
- 管理员 → 系统：上传图片、地点信息、安全提醒内容
- 系统 → 管理员：检测结果、统计数据、报表
- 学生/游客 → 系统：查询请求
- 系统 → 学生/游客：大屏数据、日历数据、排行榜
-->

```
┌─────────────────┐
│                  │
│    管理员        │◄────────────────┐
│                  │                 │
└────────┬────────┘                  │
         │                           │
         │ 上传图片、管理安全提醒等     │
         ▼                           │
┌──────────────────────────┐         │
│                          │         │
│  校园流浪动物观测关爱系统  │────────►│
│                          │         │
└────────┬─────────────────┘         │
         │                           │
         │ 查询大屏/日历/排行榜        │
         ▼                           │
┌─────────────────┐                  │
│                  │                 │
│  学生/游客       │─────────────────►│
│                  │
└─────────────────┘
```

### 0层图

<!-- TODO：将系统分解为3-4个主要处理过程，画出数据流 -->

主要处理过程：

1. **图片检测管理** — 接收上传 → YOLO检测 → 入库
2. **数据查询服务** — 各类查询请求 → 返回结果
3. **安全提醒管理** — 生成/发布/下架安全提醒
4. **报表导出** — 聚合统计 → 导出CSV/JSON

```
<!-- 用文字描述各处理之间的数据流 -->
```

---

## 2.3 类图

<!-- TODO：画出系统中的实体类及其关系 -->

主要类：

| 类名 | 属性 | 方法 |
|:----|:-----|:-----|
| User | id, username, password_hash, role, created_at | login(), logout() |
| Location | id, name, description, safety_tip, created_at | add(), update(), delete() |
| Detection | id, location_id, image_path, detect_time, result_json, total_animals | upload(), getDetail(), delete() |
| SafetyTip | id, location_id, title, content, status, created_at | create(), publish(), archive() |

```
<!-- 类之间关系 -->
User ──── 创建 ────> Detection
Detection ──── 属于 ────> Location
Location ──── 拥有 ────> SafetyTip
```

---

## 2.4 ER图 + 数据字典

### ER图

```
<!-- TODO：用文字或mermaid画出ER图 -->

┌──────────┐     ┌──────────────┐     ┌──────────┐
│   User   │     │  Detection   │     │ Location │
├──────────┤     ├──────────────┤     ├──────────┤
│ id (PK)  │     │ id (PK)      │     │ id (PK)  │
│ username │────►│ location(FK) │◄────│ name     │
│ password │     │ image_path   │     │ desc     │
│ role     │     │ detect_time  │     │ safety   │
│ created  │     │ result_json  │     │ created  │
└──────────┘     │ total_animals│     └──────────┘
                 └──────────────┘           │
                                           │
                 ┌──────────────┐           │
                 │  SafetyTip   │           │
                 ├──────────────┤           │
                 │ id (PK)      │           │
                 │ location(FK) │◄──────────┘
                 │ title        │
                 │ content      │
                 │ status       │
                 │ created_at   │
                 └──────────────┘
```

### 数据字典

<!-- TODO：补充完整每个字段的类型、长度、约束 -->

**表1：user（用户）**

| 字段 | 类型 | 空 | 默认 | 说明 |
|:----|:----|:--|:----|:-----|
| id | INTEGER PK | NOT NULL | 自增 | 用户ID |
| username | TEXT UNIQUE | NOT NULL | | 用户名 |
| password_hash | TEXT | NOT NULL | | 密码哈希 |
| role | TEXT | | 'admin' | 角色 |
| created_at | DATETIME | | NOW | 创建时间 |

**表2：location（地点）**

| 字段 | 类型 | 空 | 默认 | 说明 |
|:----|:----|:--|:----|:-----|
| id | INTEGER PK | NOT NULL | 自增 | 地点ID |
| name | TEXT | NOT NULL | | 地点名称 |
| description | TEXT | | NULL | 地点描述 |
| safety_tip | TEXT | | NULL | 当前安全提醒 |
| created_at | DATETIME | | NOW | 创建时间 |

**表3：detection（检测记录）**

<!-- TODO：补充完整 -->

**表4：safety_tip（安全提醒）**

<!-- TODO：补充完整 -->

---

## 3. 非功能性需求

### 3.1 开发环境

| 项目 | 内容 |
|:----|:-----|
| 操作系统 | Windows 11 / Linux |
| 编程语言 | Python 3.12+ |
| 后端框架 | FastAPI |
| 前端框架 | Vue 3 |
| 数据库 | SQLite 3 |
| 版本控制 | Git + GitHub |
| 包管理 | pip / npm |
| AI模型 | YOLOv8n（Ultralytics） |
| IDE | VS Code |

### 3.2 运行环境

<!-- TODO：说明系统部署后需要的运行环境 -->

**服务器端：**

| 项目 | 最低配置 | 推荐配置 |
|:----|:--------|:--------|
| CPU | 4核 | 8核 |
| 内存 | 8GB | 16GB |
| GPU | 可选（非必须） | NVIDIA GTX 1060+ |
| 磁盘 | 10GB | 20GB |
| Python版本 | 3.12+ | 3.12+ |

**客户端：**

| 项目 | 要求 |
|:----|:-----|
| 浏览器 | Chrome 90+ / Edge 90+ / Safari 15+ |
| 屏幕分辨率 | 1920×1080 或以上 |

### 3.3 系统依赖项

**Python 依赖：**

<!-- TODO：列出核心Python包 -->

| 包名 | 用途 |
|:----|:-----|
| ultralytics | YOLOv8模型加载与推理 |
| fastapi | RESTful API框架 |
| uvicorn | ASGI服务器 |
| opencv-python | 图像处理 |
| jinja2 | 模板渲染 |
| sqlite3 | 数据库（Python内置） |

**Node.js 依赖（前端）：**

| 包名 | 用途 |
|:----|:-----|
| vue 3 | 前端框架 |
| echarts | 图表库 |
| naive-ui | UI组件库 |
| axios | HTTP请求 |

### 3.4 性能需求

<!-- TODO：根据实际情况调整数字 -->

| 指标 | 要求 |
|:----|:-----|
| 单张图片检测时间 | < 3秒（GPU） / < 10秒（CPU） |
| 页面加载时间 | < 2秒 |
| 并发用户数 | 支持10人同时访问 |
| 数据库记录量 | 支持10,000+条检测记录 |
| 系统可用性 | 工作日 8:00-18:00 可运行 |

### 3.5 安全性需求

<!-- TODO：简要说明安全问题 -->

- 管理员登录需要密码验证
- 用户密码不存明文，存哈希值
- 上传文件做类型校验（仅允许图片格式）
- API接口做基本鉴权

---

写完 push 到 GitHub 即可。
